import React from "react";

export default function SplashScreen() {
    return (
        <main className="flex items-center justify-center min-h-screen">
            <h1 className="text-4xl text-airbus-green">LOADING WEBSITE<br />(MAX 3 SECONDS)</h1>
        </main>
    );
}
